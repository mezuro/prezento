language: ruby
rvm:
  - 2.0.0-p195

before_script:
  - "rake db:migrate RAILS_ENV=test"

notifications:
  email:
    recipients:
      - mezuro-core@lists.ime.usp.br
    on_success: change
    on_failure: always
