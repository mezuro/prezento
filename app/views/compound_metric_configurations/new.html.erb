<div class="page-header">
  <h1> <%= t('title_compound_new') %> </h1>
</div>

<%= render partial: 'created_metrics', locals: {metric_configurations: @metric_configurations} %>

<br>

<%= form_for(@compound_metric_configuration, :url => kalibro_configuration_compound_metric_configurations_path(@kalibro_configuration_id)) do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
<% end %>

<script type="text/javascript">
  //Loads the accordion
  $(function() {
    $( "#created-metrics-accordion" ).accordion({
      heightStyle: "content",
      collapsible: true,
    });
  });
</script>
