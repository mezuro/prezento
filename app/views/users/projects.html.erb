<div class="page-header">
  <h1><%= @user.name %> Projects</h1>
</div>
<p data-toggle="tooltip" data-placement="left" title="Create a new project" >
	<%= link_to 'New Project', new_project_path, class: 
	'btn  btn-primary' %>
</p>
<%= render partial: 'shared/project_list', locals: {projects: @user.projects} %>