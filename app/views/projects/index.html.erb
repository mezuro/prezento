<div class="page-header">
  <h1>Projects</h1>
</div>

<p><% if user_signed_in? %><%= link_to 'New Project', new_project_path, class: 'btn  btn-primary' %><% end %></p>

<%= render partial: 'shared/project_list', locals: {projects: @projects} %>
