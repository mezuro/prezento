<p>
  <strong>Name:</strong>
  <%= @project.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @project.description %>
</p>

<h2>Repositories</h2>

<%= link_to 'New Repository', new_project_repository_path(@project_id)%>

<table border="1" width="30%">
  <thead>
    <tr>
      <th>Name</th>
      <th>Type</th>
    </tr>
  </thead>

  <tbody>
    <% @project_repositories.each do |repository| %>
      <tr>
        <td align="center"><%= repository.name %></td>
        <td align="center"><%= repository.type %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p>
  <%= link_to 'Destroy', project_path(@project.id), method: :delete, data: { confirm: 'Are you sure?' } %>
</p>

<%= link_to 'Back', projects_path %>
